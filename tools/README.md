# tools

## Purpose
Developer utilities (format, snapshot, data fixes) not used in runtime.

## Entrypoints
- `tools/*.py` — one-off helpers
- Notebooks/scripts should write outputs to `artifacts/` or `reports/`

## Do-not-touch
- Never auto-run in CI unless explicitly opted-in
