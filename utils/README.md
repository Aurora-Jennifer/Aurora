# utils

## Purpose
Small, pure utilities shared across modules.

## Entrypoints
- `utils/http_defaults.py` — HTTP defaults and helpers
- `utils/indicators.py` — indicator functions

## Do-not-touch
- No side effects; no network calls; pure functions only
