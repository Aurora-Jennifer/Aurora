features:
  - { name: "ret_1d_lag1", tags: ["carry"] }
  - { name: "sma_10", tags: ["trend"] }
  - { name: "sma_20", tags: ["trend"] }
  - { name: "vol_10", tags: ["vol"] }
  - { name: "rsi_14", tags: ["momentum"] }
  # NEW MOMENTUM FEATURES (discovered to improve IC from 0.08 → 0.36)
  - { name: "momentum_3d", tags: ["momentum"] }
  - { name: "momentum_5d", tags: ["momentum"] }
  - { name: "momentum_10d", tags: ["momentum"] }
  - { name: "momentum_20d", tags: ["momentum"] }
  - { name: "momentum_strength", tags: ["momentum", "interaction"] }

groups:
  - "carry"
  - "trend" 
  - "vol"
  - "momentum"

# Ablation settings
ablation:
  min_ic_improvement: 0.005  # Minimum IC improvement to flag as "harmful"
  significance_level: 0.05   # p-value threshold for statistical significance
