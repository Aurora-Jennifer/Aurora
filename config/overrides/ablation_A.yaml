# Ablation A: Baseline (no residuals, no volfix)
features:
  residuals: false
  volfix: false
  allowlist: results/production/features_whitelist.json
train:
  horizon: 10
  embargo_days: 3
  device: cuda
  random_seed: 42
eval:
  costs: base
  slippage_bps: 5
  fee_per_share: 0.001
notes: |
  Baseline configuration for ablation study.
  No residualization, no volatility fixes.
  Using safe feature allowlist from leakage elimination.
