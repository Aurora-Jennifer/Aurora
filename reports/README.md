# reports

## Purpose
Human-readable outputs and artifacts from runs (metrics, logs, summaries).

## Entrypoints
- `reports/run.json` — run header / lineage
- `reports/metrics/` — metrics per fold or run

## Do-not-touch
- Do not store secrets; redact sensitive values as `[redacted]`

