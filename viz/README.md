# viz

## Purpose
Plots and dashboards; no training or business logic.

## Entrypoints
- `viz/*.py` — visualizers
- `reports/dashboard/` — rendered outputs

## Do-not-touch
- Read-only inputs; write outputs to `reports/` or `artifacts/`

